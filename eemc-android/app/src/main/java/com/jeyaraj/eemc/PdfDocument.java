package com.jeyaraj.eemc;

/** Simple native alias so FormulaLibraryActivity can use Android's built-in PDF engine. */
public class PdfDocument extends android.graphics.pdf.PdfDocument {
    public PdfDocument() { super(); }
}
